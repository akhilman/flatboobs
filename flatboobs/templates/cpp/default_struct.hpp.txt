/* {{ default_class }} */

class {{ default_class }} : public {{ iface_class }}{
public:

  // Getters
{% for field in fields %}
  {{ field.value.type|to_cpp_type }} {{
    utils.escape(field.name) }}() const override {
    return {{ field.value|default_value }};
  }
{% endfor %}

  // Builder
  flatboobs::content_id_t content_id () const override { return 0; }
{% if not struct_def.fixed %}
  flatbuffers::Offset<{{ flatbuffer_class }}> build(
    flatbuffers::FlatBufferBuilder &_fbb,
    std::map<flatboobs::content_id_t, flatbuffers::uoffset_t> &_offset_map
    ) const override{ return flatbuffers::Offset<{{ flatbuffer_class }}>(); }

{% endif %}
};


{#
// vim: syntax=cpp
// vim: tabstop=2
// vim: shiftwidth=2
#}
