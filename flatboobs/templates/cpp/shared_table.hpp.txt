/* {{ shared_type }} */

class {{ shared_type }} : public {{ iface_class }}{
public:

  // Constructors
  explicit {{ shared_type }}(const {{ iface_class }} &_table) {
    {{ shared_type }} shared = _table.clone_shared();
    std::swap(*this, shared);
    assert(ptr_.get());
  }
  explicit {{ shared_type }}(std::shared_ptr<const {{ iface_class }}> _table) {
    ptr_ = _table;
    assert(ptr_.get());
  }

  // Clone
  {{ shared_type }} clone_shared() const override {
    return {{ shared_type }}(*this);
    }

  // Getters
{% for field in fields %}
  {{ field.value.type|to_cpp_type }} {{
    utils.escape(field.name) }}() const override
  { return ptr_->{{ utils.escape(field.name) }}(); }
{% endfor %}

  // Builder
  size_t content_id () const override { return ptr_->content_id(); }

private:
  std::shared_ptr<const {{ iface_class }}> ptr_;

};


{#
// vim: syntax=cpp
// vim: tabstop=2
// vim: shiftwidth=2
#}
