namespace fbtest.schema;

enum TestEnumFlag:ushort (bit_flags) { Foo, Bar, Buz=5 }

enum TestEnumEnum:byte { Foo, Bar, Buz=5 }

table TestEnum {
    test_flag: TestEnumFlag;
    test_enum: TestEnumEnum = Buz;
}

root_type TestEnum;
file_identifier "TENM";
